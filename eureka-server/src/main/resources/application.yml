server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    ###是否注册到注册中心
    registerWithEureka: false
    ###是否到注册中心获取服务信息
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/